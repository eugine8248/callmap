<!-- Thanks for sending a PR! A short note on what's helpful:

  - One commit per logical change (squash is fine on merge).
  - Run `npm run typecheck` and `npm run build` locally first.
  - If you touched the parser or callgraph builder, ideally bring a PR URL
    that exercises the change.
-->

## What

Briefly describe what the PR does.

## Why

What problem does it solve? Link an issue if there is one.

## How (optional)

Implementation notes only if they're non-obvious.

## Verification

- [ ] `npm run typecheck` clean
- [ ] `npm run build` clean
- [ ] If applicable: opened a PR URL that exercises the change and confirmed the graph renders as expected
- [ ] If applicable: added/updated docs in `packages/site/src/pages/docs.astro`

## Screenshots (if it's a UI change)

<!-- Drag images here. -->
