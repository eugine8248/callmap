# Force npm-run-script to use bash on Windows.
# Without this, npm 10.9 + Node 22 + Git-Bash exhibits a known issue
# where cmd-shim binaries (e.g. tsc.cmd) report exit code 1 even on
# success. Using bash to spawn the script avoids the cmd wrapper.
script-shell=bash
