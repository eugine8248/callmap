{
  "name": "@callmap/core",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "callmap engine — PR fetch, parsing, callgraph build, diff, layout. UI-agnostic.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./parseWorker": "./src/parseWorker.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "dagre": "^0.8.5",
    "web-tree-sitter": "^0.24.7"
  },
  "devDependencies": {
    "@types/dagre": "^0.7.52",
    "typescript": "^5.6.3"
  }
}
